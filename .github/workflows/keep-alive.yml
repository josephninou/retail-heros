name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # S'exécute toutes les 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -s https://retail-heros.onrender.com/ > /dev/null
